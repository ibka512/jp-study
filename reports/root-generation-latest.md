# 钟日英语词根词缀生成报告

- 生成时间：2026-07-19T19:18:01.900563+00:00
- 模型：`deepseek-v4-flash`
- 等级：CET-4
- 本次检查：100
- 有效拆分：65
- 不适合可靠拆分：33
- 未通过校验：2
- API 请求：11
- 输入 Token：10755
- 输出 Token：7692
- 处理前待检查：3897
- 处理后待检查：3799

## 拆分样例

- `explosive`：explos(爆炸)-ive(有...性质的)
- `numerous`：numer(数字)-ous(充满...的)
- `analyse`：ana(分开)-lyse(松开)
- `discourage`：dis(否定)-courage(勇气)
- `resemble`：re(再)-semble(相似)
- `remote`：re(再)-mote(移动)
- `pollution`：pollut(污染)-ion(行为/状态)
- `pretend`：pre(预先)-tend(伸展)
- `calculate`：calcul(计算)-ate(使...)
- `persistent`：per(贯穿)-sist(站立)-ent(有...性质的)
- `conceal`：con(共同)-ceal(隐藏)
- `audience`：aud(听)-ience(人/物)
- `meanwhile`：mean(中间)-while(时间)
- `competent`：compet(竞争)-ent(形容词后缀)
- `investment`：invest(投资)-ment(名词后缀)
- `optional`：option(选择)-al(形容词后缀)
- `analysis`：ana(向上)-lysis(分解)
- `fashionable`：fashion(时尚)-able(可...的)
- `apparent`：appar(出现)-ent(形容词后缀)
- `journalist`：journal(日报)-ist(人)
- `exposure`：ex(向外)-pos(放置)-ure(名词后缀)
- `protective`：protect(保护)-ive(形容词后缀)
- `multicultural`：multi(多)-cultural(文化的)
- `sideways`：side(边，侧面)-ways(方向，方式)
- `independent`：in(不)-depend(依赖)-ent(形容词后缀)
- `carriage`：carri(运送)-age(名词后缀)
- `infinite`：in(不)-finite(有限的)
- `concede`：con(共同)-cede(走，让步)
- `weekly`：week(周)-ly(每…的)
- `result`：re(回)-sult(跳)
- `selfish`：self(自己)-ish(…的)
- `sexism`：sex(性别)-ism(主义)
- `commission`：com(共同)-miss(送)-ion(名词后缀)
- `headline`：head(头)-line(线)
- `connect`：con(共同)-nect(连接)
- `rational`：ration(理性)-al(形容词后缀)
- `editorial`：editor(编辑)-ial(形容词后缀)
- `resume`：re(再次)-sume(拿取)
- `rebuild`：re(再次)-build(建造)
- `namely`：name(名字)-ly(方式)

## 未通过项目

- `plentiful`：词素无法原样拼回 plentiful
- `besides`：词素格式无效：s
