# 钟日英语词根词缀生成报告

- 生成时间：2026-07-19T19:26:49.748106+00:00
- 模型：`deepseek-v4-flash`
- 等级：CET-4
- 本次检查：100
- 有效拆分：46
- 不适合可靠拆分：54
- 未通过校验：0
- API 请求：14
- 输入 Token：16940
- 输出 Token：11649
- 处理前待检查：3897
- 处理后待检查：3797

## 拆分样例

- `explosive`：explos(爆炸)-ive(有...性质的)
- `numerous`：numer(数字)-ous(充满...的)
- `discourage`：dis(否定)-courage(勇气)
- `pollution`：pollut(污染)-ion(行为/状态)
- `calculate`：calcul(计算)-ate(使成为)
- `persistent`：per(贯穿)-sist(站立)-ent(具有...性质的)
- `audience`：aud(听)-ience(人/物)
- `meanwhile`：mean(中间)-while(时间)
- `investment`：invest(投资)-ment(名词后缀)
- `optional`：option(选择)-al(形容词后缀)
- `fashionable`：fashion(时尚)-able(可...的)
- `journalist`：journal(日报)-ist(人)
- `protective`：protect(保护)-ive(形容词后缀)
- `multicultural`：multi(多)-cultural(文化的)
- `independent`：in(不)-depend(依赖)-ent(形容词后缀)
- `carriage`：carri(运送)-age(名词后缀)
- `infinite`：in(不)-finite(有限的)
- `concede`：con(共同)-cede(走，让步)
- `weekly`：week(周)-ly(每…的)
- `selfish`：self(自己)-ish(形容词后缀)
- `sexism`：sex(性别)-ism(主义，偏见)
- `commission`：com(共同)-miss(送)-ion(名词后缀)
- `headline`：head(头)-line(行)
- `connect`：con(共同)-nect(连接)
- `rational`：ration(理性)-al(形容词后缀)
- `editorial`：editor(编辑)-ial(形容词后缀)
- `rebuild`：re(再次)-build(建造)
- `artistic`：art(艺术)-istic(具有...性质的)
- `consultant`：consult(咨询)-ant(人)
- `sunset`：sun(太阳)-set(落下)
- `disguise`：dis(否定)-guise(外表)
- `liberal`：liber(自由)-al(具有...性质的)
- `violent`：viol(暴力)-ent(具有...性质的)
- `resolve`：re(再次)-solve(解决)
- `confront`：con(共同，一起)-front(前面，正面)
- `dominate`：domin(统治)-ate(使成为)
- `imitate`：imit(模仿)-ate(使成为)
- `faithful`：faith(信任，信念)-ful(充满…的)
- `reproduce`：re(再次)-produce(生产)
- `institution`：in(进入)-stitut(建立)-ion(行为、状态)

## 未通过项目

- 无
